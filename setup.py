from setuptools import setup, find_packages

setup(
    name='ReCatcher',
    version='0.1',
    description='Regression Testing Tool for Code LLM',
    author='Altaf Allah Abbassi',
    packages=find_packages(),  
    install_requires=[  
    ],
    python_requires='>=3.9',  # Specify minimum Python version
)
